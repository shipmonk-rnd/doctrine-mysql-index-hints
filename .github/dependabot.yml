version: 2
updates:
    -
        package-ecosystem: "composer"
        directory: "/"
        schedule:
            interval: "monthly"
        versioning-strategy: lockfile-only
        allow:
            - dependency-type: "development"
        groups:
            dev-dependencies:
                dependency-type: development
